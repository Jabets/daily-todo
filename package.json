{
  "name": "daily-todo",
  "version": "1.0.0",
  "description": "Daily Todo App with Frontend-Backend Separation",
  "scripts": {
    "start": "cd server && npm install && node index.js",
    "server": "cd server && node index.js"
  }
}
